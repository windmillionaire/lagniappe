# 13 — One requested task covering four subjects

Operator-only. Stress respecting the user's requested unit of work even when a
supporting file and the workspace mention several suitable subject Pages.

## Preconditions and expected result

- The actor can create an open Task and attach its note in a sensible apiary or
  beekeeping context. Record available parent/work-type choices; no particular
  incidental parent label is required if one task covers the intended work.
- Propose **one** winterization Task for the four hives, not four per-hive Tasks
  merely because those Pages exist. Preserve all requested work: entrance
  reducers, mouse guards, varroa treatment with its treatment date recorded,
  checking at least 40 lb stored honey per hive, and strapping the outer hive.
- Inspect the full note and attach it once to the Task with a grounded summary.
  Finding useful workspace details does not excuse silently dropping the file.
- The winterization work is open. The August 30, 2026 inspection is not its
  completion date; “before the first frost” supplies no exact deadline. Do not
  invent a date or treat the queen-right inspection as completed winterization.
- Submit a ready reviewable proposal/link without execution. A local-only
  checklist is not fulfillment of the explicit Lagniappe request.

## Historical evidence and controls

Source: `/home/swifty/Desktop/FILE_USE_AI_TEST/file-use-ai-after/05-apiary-task-create`.
The historical external run failed: four Tasks and no uploaded/attached note,
despite an accepted fileless proposal. It used a stale skill; on-site evidence
also exposed then-existing MIME/completion problems. See `RESULTS.md`,
`artifacts/archive.jsonl`, and `SESSION.txt` there. Do not infer that those
implementation defects remain present today.

The fixture is byte-identical. The new canonical natural prompt adds “in
Lagniappe” and drops the historical absolute file path, removing an avoidable
intent ambiguity without prescribing tools. This is a **new prompt revision**:
the Pi/REST run is historical reference, not a same-prompt or current MCP
baseline. No hidden four-Task expectation and no outdated routing requirement.
