# 10 — Repeat three weeks after completion

Operator-only. Keep this alongside case 03: removing invalid one-time schedules
must not remove valid recurring reminders.

## Preconditions and expected result

- The actor has an editable personal Page. No attachments are needed.
- Propose one open reminder to clean the coffee grinder, first due October 12,
  2026, on that Page.
- Its recurring schedule is **three weeks after completion**, not every fixed
  third calendar week regardless of completion and not a one-time reminder.
  Equivalent accepted representations count; an omitted completion field is
  fine when the documented default is open.
- Submit one ready reviewable proposal with a usable link. Do not execute it,
  simulate completion in the living workspace, or claim it is already active.

## Observe and qualify

Round 2 passed immediately using inline actor/contract context; redundant
inventory or actor reads are not part of the required procedure. Its starter
output clipped in the host despite a correct outcome, so watch discovery and
response presentation without treating every clipped output as lost evidence.

Prompt unchanged from the same-named Desktop case; see
[review](../../comparisons/mcp-round-2.md).
Actual future completion/rescheduling was not exercised by that proposal-only
run. Cover the mechanics deterministically or request separate disposable
execution authority. Refresh fixed dates only as a versioned new input set,
not during a paired comparison.
