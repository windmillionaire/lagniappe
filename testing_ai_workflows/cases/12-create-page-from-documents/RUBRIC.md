# 12 — Three documents, one new subject

Operator-only. Retain this historical semantic case because explicit creation
with several supporting files differs from generic “file everything” sorting.

## Preconditions and expected result

- The actor can create a Page and attach files in a suitable home/equipment
  destination. Record whether an earlier Cider Press proposal was executed;
  this case intends a new subject, not duplicates of an existing appliance.
- Propose one Page representing the Meadowline 6-gallon cider press, model MP-6,
  serial MP6-2029. A concise or more specific name is equally acceptable.
- Inspect and attach all three files to that Page, with one grounded summary
  per file. Do not create a Page for each document or omit warranty/setup data.
- Preserve purchase date August 29, 2026, $412 purchase, two-year manufacturing
  defect warranty from purchase, registration deadline October 1, 2026, and
  first-use preparation. Setup instructions are **not completed work**.
- A suitable existing Category/Form is useful; no new broad overview structure
  or fabricated service event. A grounded optional follow-up is reviewable
  variance, not a substitute for the requested Page.
- Submit a ready reviewable proposal/link without execution. Judge semantics
  against current public capabilities; obsolete file-backed Create routing
  restrictions are not requirements.

## Historical evidence and controls

Source: `/home/swifty/Desktop/FILE_USE_AI_TEST/file-use-ai-after/02-page-from-documents`.
The historical Pi/REST run passed semantically, with a wrong-tree-path control
gap; the three files were byte-identical. Evidence is `RESULTS.md`,
`artifacts/transcript.jsonl`, and `artifacts/SESSION.txt` there.

Fixtures are copied unchanged. The canonical prompt retains the historical
natural request but drops its absolute Desktop paths and procedural scorer
preamble; the old actual user message appended three before-tree file paths.
Treat that run as a historical reference, **not** a controlled same-prompt MCP
baseline. Launch in this case's fixtures directory so only the three inputs
are in the working folder.
