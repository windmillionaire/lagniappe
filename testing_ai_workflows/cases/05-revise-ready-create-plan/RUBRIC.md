# 05 — Revise the same proposal

Operator-only. Send the two natural messages in order, waiting for the first
answer before the follow-up. Stress complete resubmission and continuity.

## Preconditions and expected result

- The actor has an editable personal Page. No attachments are needed.
- The first ready proposal contains two separate open Tasks: confirm final
  cover-crop and pollinator seed quantities by October 23, 2026, then send the
  confirmed order to Meadow County Co-op by October 26, 2026.
- The follow-up updates the co-op deadline to October 28, 2026 in the **same
  Plan**. Preserve both Tasks and all unaffected facts; summaries may change
  to reflect the new deadline. Do not replace a full proposal with a partial
  patch or create a second report.
- Both submissions are accepted for review, with the revised receipt referring
  to the revised complete proposal. No execution or claim of execution.
- Execution ordering between proposed actions is not evidence of a persistent
  dependency between the resulting Tasks; do not invent that relationship.

## Evidence and controls

Prompt file is unchanged from the same-named Desktop case. Round 2 passed,
using bundled context rather than a separate contract read before each submit.
Its initial user message omitted “in Lagniappe,” added in a steering message;
keep that historical prompt intervention explicit rather than copying it into
the canonical two-message test. The seed-order screenshot was saved under
Desktop case 06, not case 05. See
[review](../../comparisons/mcp-round-2.md).
Record client/model settings for both turns and exclude between-turn idle and
export activity from task-only comparisons. Version fixed-date refreshes.
